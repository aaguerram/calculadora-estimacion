{
  "name": "calculadora",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "oxlint",
    "lint:fsd": "steiger ./src",
    "check": "npm run lint && npm run lint:fsd && npm run lint:marca && tsc -b && npm run test",
    "token": "node scripts/dev-token.mjs",
    "stack:up": "npm run token:env && docker compose up -d --build",
    "stack:down": "docker compose down",
    "stack:reset": "docker compose down -v && npm run token:env && docker compose up -d --build",
    "stack:logs": "docker compose logs -f",
    "test": "vitest run",
    "test:watch": "vitest",
    "token:env": "node scripts/dev-token.mjs --write",
    "seed": "node scripts/seed-alcance.mjs",
    "test:e2e": "vitest run --config vitest.e2e.config.ts",
    "seed:all": "npm run seed && npm run benchmark:cargar",
    "benchmark:cargar": "node scripts/cargar-benchmark.mjs",
    "ui:revisar": "node scripts/revisar-ui.mjs",
    "benchmark:descargar": "node scripts/descargar-datasets.mjs",
    "benchmark": "npm run benchmark:descargar && npm run benchmark:cargar",
    "diagnostico": "node scripts/diagnostico.mjs",
    "lint:marca": "node scripts/verificar-marca.mjs"
  },
  "dependencies": {
    "@carbon/react": "^1.116.0",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "react-router": "^8.4.0"
  },
  "devDependencies": {
    "@feature-sliced/steiger-plugin": "^0.7.0",
    "@types/node": "^26.6.2",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "@vitest/coverage-v8": "^5.0.1",
    "oxlint": "^1.84.0",
    "playwright": "^1.63.0",
    "sass-embedded": "^1.104.1",
    "steiger": "^0.6.0",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
